name: 'Pull Request'

on:
  pull_request:
    branches:
      - master

jobs:
  build and test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: 11
      run: gradle clean build test